{
  "name": "7ink-website",
  "version": "1.0.0",
  "description": "7ink.com.au - Knight Theme Website",
  "scripts": {
    "build": "node scripts/prepare-cloudflare-assets.mjs",
    "dev": "echo \"Use local XAMPP Apache for development\"",
    "start": "echo \"Static deployment on Vercel\"",
    "deploy": "npm run build && npx wrangler deploy --config wrangler.jsonc",
    "deploy:dry-run": "npm run build && npx wrangler deploy --config wrangler.jsonc --dry-run",
    "typecheck": "tsc --noEmit"
  },
  "keywords": [
    "7ink",
    "website"
  ],
  "author": "7ink",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.20.1",
    "typescript": "^5.9.3"
  }
}
